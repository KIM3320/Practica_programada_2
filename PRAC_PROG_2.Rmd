---
title: "PRAC_PROG_2"
author: "Kimberly Matarrita"
date: "2023-11-29"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(shiny)
library(DT)
library(ggplot2)
library(shiny)
library(leaflet)
library(readr)
library(janitor)
library(dplyr)
library(stringr)
library(plotly)
library(gt)
library(waiter)
library(readr)
```

```{r}
# Cargar el conjunto de datos
datos_libertad <- read_csv("datos_libertad.csv")
View(datos_libertad)

```
```{r}



ui <- fluidPage(
  
 
  titlePanel("Visualización de Datos de Libertad en Países"),
  
 
  sidebarLayout(
    sidebarPanel(
      selectInput("country", "Seleccionar País:", choices = unique(data$country)),
      sliderInput("yearRange", "Rango de Años:",
                  min = 2000, max = 2023, value = c(2000, 2023)),
      radioButtons("dataType", "Tipo de Datos:",
                   choices = c("Ranking", "Puntaje"))
    ),
    
 
    mainPanel(
      plotOutput("freedomPlot"),
      downloadButton("downloadData", "Descargar Datos")
    )
  )
)


server <- function(input, output) {
  
  
  output$freedomPlot <- renderPlot({
    
  })
  
  output$downloadData <- downloadHandler(
    filename = function() {
      paste("data-", input$country, ".csv", sep = "")
    },
    content = function(file) {
 
    }
  )
}


shinyApp(ui = ui, server = server)

```



